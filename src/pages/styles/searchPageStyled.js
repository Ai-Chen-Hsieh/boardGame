import styled from "styled-components";

const SearchPageStyled = styled.div`
    min-height: calc(100% - 300px);
    margin: 1rem auto;
    h3 {
        color: #adadad;
        margin: 0.5em 1em;
    }
`
export {
    SearchPageStyled
}